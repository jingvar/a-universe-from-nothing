---
- name: Add public FQDN to /etc/hosts
  hosts: all
  tasks:
    - name: Add public fqdn and vip /etc/hosts
      become: yes
      lineinfile:
        path: /etc/hosts
        line: "{{ public_vip_address }} {{ public_fqdn  }}"
